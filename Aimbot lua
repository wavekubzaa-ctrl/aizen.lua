--[[ 
    PROJECT: ELITE HAMBURG (V63 - THE FINAL FIX)
    FIXES: ESP Team Color (Now using TeamColor), Restored Safe Exit
    STABILITY: Locked all functions to prevent disappearance
--]]

-- // Cleanup //
for _, ui in pairs(game.CoreGui:GetChildren()) do
    if ui.Name:find("Elite_V") then ui:Destroy() end
end

local _LP = game.Players.LocalPlayer
local _CAMERA = workspace.CurrentCamera
local _RUN = game:GetService("RunService")
local _LIGHTING = game:GetService("Lighting")

getgenv().EliteConfig = {
    AimEnabled = false,
    AutoShoot = false,
    AimPart = "HumanoidRootPart", 
    FOV = 200,
    CurrentMode = "G36",
    BulletSpeed = 1100,
    Smoothness = 0.08,
    ESPEnabled = true,
    ArrestAssist = false,
    VehicleBoost = false,
    NoFog = false,
    SpeedValue = 300,
    ThemeColor = Color3.fromRGB(0, 255, 180)
}

local WeaponSettings = {
    ["G36"] = {Speed = 1100, Color = Color3.fromRGB(0, 255, 180)},
    ["Sniper"] = {Speed = 3500, Color = Color3.fromRGB(255, 50, 50)},
    ["Glock 17"] = {Speed = 950, Color = Color3.fromRGB(255, 200, 0)}
}

-- // UI Builder //
local ScreenGui = Instance.new("ScreenGui", game.CoreGui); ScreenGui.Name = "Elite_V63"
local MainBtn = Instance.new("TextButton", ScreenGui)
MainBtn.Size = UDim2.new(0, 60, 0, 60); MainBtn.Position = UDim2.new(0, 20, 0.4, 0); MainBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15); MainBtn.Text = "ELITE\nV63"; MainBtn.TextColor3 = getgenv().EliteConfig.ThemeColor; MainBtn.Font = Enum.Font.GothamBold; MainBtn.Draggable = true; Instance.new("UICorner", MainBtn).CornerRadius = UDim.new(1, 0)
local Stroke = Instance.new("UIStroke", MainBtn); Stroke.Color = getgenv().EliteConfig.ThemeColor; Stroke.Thickness = 2

local Panel = Instance.new("Frame", MainBtn)
Panel.Size = UDim2.new(0, 210, 0, 420); Panel.Position = UDim2.new(1, 15, 0, -180); Panel.BackgroundColor3 = Color3.fromRGB(25, 25, 30); Panel.Visible = false; Instance.new("UICorner", Panel)
local Scroll = Instance.new("ScrollingFrame", Panel)
Scroll.Size = UDim2.new(1, -10, 1, -20); Scroll.Position = UDim2.new(0, 5, 0, 10); Scroll.BackgroundTransparency = 1; Scroll.CanvasSize = UDim2.new(0, 0, 0, 600); Scroll.ScrollBarThickness = 2
Instance.new("UIListLayout", Scroll).Padding = UDim.new(0, 5)

local function AddToggle(text, config_key)
    local b = Instance.new("TextButton", Scroll)
    b.Size = UDim2.new(1, -10, 0, 35); b.BackgroundColor3 = Color3.fromRGB(45, 45, 50); b.Text = text .. (getgenv().EliteConfig[config_key] and ": ON" or ": OFF"); b.TextColor3 = Color3.new(1, 1, 1); b.Font = Enum.Font.SourceSansBold; Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function()
        getgenv().EliteConfig[config_key] = not getgenv().EliteConfig[config_key]
        b.Text = text .. (getgenv().EliteConfig[config_key] and ": ON" or ": OFF")
    end)
end

-- // ฟังก์ชันทั้งหมดต้องอยู่ตรงนี้ //
AddToggle("AIMLOCK", "AimEnabled")
AddToggle("AUTO SHOOT", "AutoShoot")
AddToggle("ESP PLAYER", "ESPEnabled")
AddToggle("ARREST ASSIST", "ArrestAssist")
AddToggle("VEHICLE BOOST", "VehicleBoost")
AddToggle("NO FOG", "NoFog")

local ModeBtn = Instance.new("TextButton", Scroll)
ModeBtn.Size = UDim2.new(1, -10, 0, 35); ModeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50); ModeBtn.Text = "MODE: G36"; ModeBtn.TextColor3 = Color3.new(1, 1, 1); ModeBtn.Font = Enum.Font.SourceSansBold; Instance.new("UICorner", ModeBtn)
local mIdx = 1; local Modes = {"G36", "Sniper", "Glock 17"}
ModeBtn.MouseButton1Click:Connect(function()
    mIdx = (mIdx % #Modes) + 1; local m = Modes[mIdx]
    getgenv().EliteConfig.CurrentMode = m; getgenv().EliteConfig.BulletSpeed = WeaponSettings[m].Speed
    ModeBtn.Text = "MODE: " .. m; MainBtn.TextColor3 = WeaponSettings[m].Color; Stroke.Color = WeaponSettings[m].Color
end)

-- ปุ่มออกที่หายไป
local ExitBtn = Instance.new("TextButton", Scroll)
ExitBtn.Size = UDim2.new(1, -10, 0, 35); ExitBtn.BackgroundColor3 = Color3.fromRGB(80, 20, 20); ExitBtn.Text = "SAFE EXIT"; ExitBtn.TextColor3 = Color3.new(1, 1, 1); ExitBtn.Font = Enum.Font.SourceSansBold; Instance.new("UICorner", ExitBtn)
ExitBtn.MouseButton1Click:Connect(function() _LP:Kick("Elite Hamburg - Safe Exit Successfully") end)

MainBtn.MouseButton1Click:Connect(function() Panel.Visible = not Panel.Visible end)

-- // ระบบ ESP สีตามทีมที่แม่นยำ //
local function GetPlayerColor(p)
    if p.Team then
        return p.TeamColor.Color -- ใช้สีจากทีมในเกมโดยตรง (โจรแดงก็แดงเลย)
    end
    return Color3.new(1, 1, 1)
end

local function GetTargetable(p)
    if not p.Character or not p.Character:FindFirstChild("Humanoid") or p.Character.Humanoid.Health <= 0 then return false end
    if p.Team == _LP.Team then return false end
    return true
end

-- // ESP System //
local ESP_Objects = {}
local function CreateESP(p)
    local t = Drawing.new("Text"); t.Visible = false; t.Center = true; t.Outline = true; t.Font = 2; t.Size = 13
    ESP_Objects[p] = t
end
for _, p in pairs(game.Players:GetPlayers()) do if p ~= _LP then CreateESP(p) end end
game.Players.PlayerAdded:Connect(function(p) if p ~= _LP then CreateESP(p) end end)
game.Players.PlayerRemoving:Connect(function(p) if ESP_Objects[p] then ESP_Objects[p]:Remove(); ESP_Objects[p] = nil end end)

-- // Main Update Loop //
_RUN.RenderStepped:Connect(function()
    if getgenv().EliteConfig.NoFog then _LIGHTING.FogEnd = 999999; if _LIGHTING:FindFirstChild("Atmosphere") then _LIGHTING.Atmosphere.Density = 0 end end

    for p, tag in pairs(ESP_Objects) do
        if getgenv().EliteConfig.ESPEnabled and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
            local pos, screen = _CAMERA:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
            if screen then
                local dist = math.floor((_LP.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude)
                tag.Position = Vector2.new(pos.X, pos.Y - 40); tag.Text = p.DisplayName .. " [" .. dist .. "m]"; tag.Color = GetPlayerColor(p); tag.Visible = true
            else tag.Visible = false end
        else tag.Visible = false end
    end

    if getgenv().EliteConfig.AimEnabled then
        local tool = _LP.Character and _LP.Character:FindFirstChildWhichIsA("Tool")
        if tool and (tool.Name:lower():find("gun") or tool.Name:lower():find("rifle") or tool.Name:lower():find("glock") or tool.Name:lower():find("sniper")) then
            local target, shortestMag = nil, getgenv().EliteConfig.FOV
            for _, p in pairs(game.Players:GetPlayers()) do
                if p ~= _LP and GetTargetable(p) then
                    local tPart = p.Character:FindFirstChild(getgenv().EliteConfig.AimPart)
                    if tPart then
                        local pos, screen = _CAMERA:WorldToViewportPoint(tPart.Position)
                        if screen then
                            local mag = (Vector2.new(pos.X, pos.Y) - Vector2.new(_CAMERA.ViewportSize.X/2, _CAMERA.ViewportSize.Y/2)).Magnitude
                            if mag < shortestMag then shortestMag = mag; target = p end
                        end
                    end
                end
            end
            if target then
                local tPart = target.Character[getgenv().EliteConfig.AimPart]
                local dist = (_LP.Character.HumanoidRootPart.Position - tPart.Position).Magnitude
                local predictPos = tPart.Position + (tPart.AssemblyLinearVelocity * (dist / getgenv().EliteConfig.BulletSpeed))
                _CAMERA.CFrame = _CAMERA.CFrame:Lerp(CFrame.new(_CAMERA.CFrame.Position, predictPos + Vector3.new(0, dist/800, 0)), getgenv().EliteConfig.Smoothness)
                if getgenv().EliteConfig.AutoShoot and shortestMag < 35 then tool:Activate() end
            end
        end
    end
end)

-- Vehicle Boost
_RUN.Heartbeat:Connect(function()
    if getgenv().EliteConfig.VehicleBoost then
        local seat = _LP.Character and _LP.Character:FindFirstChild("Humanoid") and _LP.Character.Humanoid.SeatPart
        if seat and seat:IsA("VehicleSeat") and seat.Throttle ~= 0 then
            seat.AssemblyLinearVelocity = seat.CFrame.LookVector * (getgenv().EliteConfig.SpeedValue * seat.Throttle) + Vector3.new(0, seat.AssemblyLinearVelocity.Y, 0)
        end
    end
end)
