--[[ 
    PROJECT: UNIVERSAL ELITE (V35.1 - ROBBERY UPDATE)
    FEATURES: Smart Predict, Criminal ESP, Auto-Shoot, Vehicle Boost
    NEW: Robbery ESP (Safe, ATM, Register) for EH
--]]

if not game:IsLoaded() then game.Loaded:Wait() end

local _LP = game.Players.LocalPlayer
local _CAMERA = workspace.CurrentCamera
local _RUN = game:GetService("RunService")
local _LIGHTING = game:GetService("Lighting")
local _INPUT = game:GetService("UserInputService")

getgenv().EliteConfig = {
    AimEnabled = false,
    AutoShoot = false,
    ESPEnabled = false,
    RobberyESP = false, -- เพิ่มใหม่
    AimPart = "HumanoidRootPart",
    FOV = 200,
    NoFog = false,
    MaxDist = 2000,
    CurrentMode = "G36",
    BulletSpeed = 850,
    VehicleTune = false,
    SpeedMult = 20,
    ThemeColor = Color3.fromRGB(255, 200, 0)
}

-- // UI Screen //
local _G = Instance.new("ScreenGui", game.CoreGui)
_G.Name = "BWH_Elite_V35_1"

local _MainBtn = Instance.new("TextButton", _G)
_MainBtn.Size = UDim2.new(0, 65, 0, 65)
_MainBtn.Position = UDim2.new(0, 20, 0.4, 0)
_MainBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
_MainBtn.Text = "AUTO-V35"
_MainBtn.TextColor3 = getgenv().EliteConfig.ThemeColor
_MainBtn.Font = Enum.Font.GothamBold
_MainBtn.TextSize = 10
_MainBtn.Draggable = true
Instance.new("UICorner", _MainBtn).CornerRadius = UDim.new(1, 0)
local _Stroke = Instance.new("UIStroke", _MainBtn)
_Stroke.Color = getgenv().EliteConfig.ThemeColor

local _Panel = Instance.new("Frame", _MainBtn)
_Panel.Size = UDim2.new(0, 200, 0, 380) -- เพิ่มขนาดบอร์ดนิดหน่อย
_Panel.Position = UDim2.new(1, 15, 0, -130)
_Panel.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
_Panel.Visible = false
Instance.new("UICorner", _Panel)

local function AddBtn(text, y, callback, color)
    local b = Instance.new("TextButton", _Panel)
    b.Size = UDim2.new(0.9, 0, 0, 28)
    b.Position = UDim2.new(0.05, 0, 0, y)
    b.BackgroundColor3 = color or Color3.fromRGB(35, 35, 35)
    b.Text = text; b.TextColor3 = Color3.new(1, 1, 1)
    b.Font = Enum.Font.SourceSansBold; b.TextSize = 14
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function() callback(b) end)
    return b
end

-- // ฟีเจอร์หลัก //
AddBtn("SMART AIM: OFF", 20, function(b)
    getgenv().EliteConfig.AimEnabled = not getgenv().EliteConfig.AimEnabled
    b.Text = getgenv().EliteConfig.AimEnabled and "AIMLOCK: ON" or "AIMLOCK: OFF"
    b.BackgroundColor3 = getgenv().EliteConfig.AimEnabled and Color3.fromRGB(0, 100, 0) or Color3.fromRGB(35, 35, 35)
end)

AddBtn("AUTO SHOOT: OFF", 55, function(b)
    getgenv().EliteConfig.AutoShoot = not getgenv().EliteConfig.AutoShoot
    b.Text = getgenv().EliteConfig.AutoShoot and "AUTO SHOOT: ON" or "AUTO SHOOT: OFF"
    b.BackgroundColor3 = getgenv().EliteConfig.AutoShoot and Color3.fromRGB(150, 100, 0) or Color3.fromRGB(35, 35, 35)
end)

AddBtn("ROBBERY ESP: OFF", 90, function(b) -- ปุ่มที่เพิ่มใหม่
    getgenv().EliteConfig.RobberyESP = not getgenv().EliteConfig.RobberyESP
    b.Text = getgenv().EliteConfig.RobberyESP and "ROBBERY ESP: ON" or "ROBBERY ESP: OFF"
    b.BackgroundColor3 = getgenv().EliteConfig.RobberyESP and Color3.fromRGB(0, 150, 150) or Color3.fromRGB(35, 35, 35)
end)

AddBtn("TARGET: BODY/HEAD", 125, function(b)
    getgenv().EliteConfig.AimPart = (getgenv().EliteConfig.AimPart == "HumanoidRootPart") and "Head" or "HumanoidRootPart"
    b.Text = "TARGET: " .. (getgenv().EliteConfig.AimPart == "Head" and "HEAD" or "BODY")
end)

AddBtn("CRIMINAL ESP: TOGGLE", 160, function() getgenv().EliteConfig.ESPEnabled = not getgenv().EliteConfig.ESPEnabled end)
AddBtn("NO FOG: TOGGLE", 195, function() getgenv().EliteConfig.NoFog = not getgenv().EliteConfig.NoFog end)
AddBtn("VEHICLE BOOST: TOGGLE", 230, function() getgenv().EliteConfig.VehicleTune = not getgenv().EliteConfig.VehicleTune end)
AddBtn("SAFE LEAVE", 280, function() _LP:Kick("Safe Leave Activated") end, Color3.fromRGB(150, 0, 0))
AddBtn("CLOSE", 320, function() _Panel.Visible = false end)

_MainBtn.MouseButton1Click:Connect(function() _Panel.Visible = not _Panel.Visible end)

-- // Check Status //
local function IsShootable(p)
    local stats = p:FindFirstChild("leaderstats")
    if stats then
        local wanted = stats:FindFirstChild("Wanted") or stats:FindFirstChild("Bounty") or stats:FindFirstChild("Stars")
        if wanted and wanted.Value > 0 then return true end
    end
    if p.Team and (p.Team.Name:find("Police") or p.Team.Name:find("Sheriff")) then return true end
    return false
end

-- // Robbery ESP Logic //
local robLabels = {}
local function CreateRobESP(obj)
    local label = Drawing.new("Text")
    label.Size = 14; label.Center = true; label.Outline = true; label.Visible = false
    robLabels[obj] = label
end

local function ScanRobbery()
    for _, obj in pairs(workspace:GetDescendants()) do
        if (obj.Name:find("Safe") or obj.Name:find("ATM") or obj.Name:find("Register")) and obj:IsA("BasePart") then
            if not robLabels[obj] then CreateRobESP(obj) end
        end
    end
end

-- // Auto Shoot Logic //
local lastShoot = 0
local function FireWeapon()
    if tick() - lastShoot > 0.1 then
        mouse1click() 
        lastShoot = tick()
    end
end

-- // Main Loop //
_RUN.RenderStepped:Connect(function()
    -- No Fog
    if getgenv().EliteConfig.NoFog then
        _LIGHTING.FogEnd = 1e6
        if _LIGHTING:FindFirstChild("Atmosphere") then _LIGHTING.Atmosphere.Density = 0 end
    end

    -- Aim & Auto Shoot Logic (คงเดิม)
    if getgenv().EliteConfig.AimEnabled then
        local target, shortestDist = nil, getgenv().EliteConfig.FOV
        local screenCenter = Vector2.new(_CAMERA.ViewportSize.X/2, _CAMERA.ViewportSize.Y/2)

        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= _LP and p.Character and p.Character:FindFirstChild(getgenv().EliteConfig.AimPart) and IsShootable(p) then
                if p.Character.Humanoid.Health > 0 then
                    local pos, screen = _CAMERA:WorldToViewportPoint(p.Character[getgenv().EliteConfig.AimPart].Position)
                    if screen then
                        local mag = (Vector2.new(pos.X, pos.Y) - screenCenter).Magnitude
                        if mag < shortestDist then shortestDist = mag; target = p end
                    end
                end
            end
        end

        if target then
            local tPart = target.Character[getgenv().EliteConfig.AimPart]
            local dist = (_LP.Character.HumanoidRootPart.Position - tPart.Position).Magnitude
            local finalPos = tPart.Position + (tPart.AssemblyLinearVelocity * (dist / getgenv().EliteConfig.BulletSpeed))
            _CAMERA.CFrame = _CAMERA.CFrame:Lerp(CFrame.new(_CAMERA.CFrame.Position, finalPos + Vector3.new(0, (dist / 500), 0)), 0.2)
            
            if getgenv().EliteConfig.AutoShoot and shortestDist < 20 then FireWeapon() end
        end
    end

    -- Robbery ESP Render
    for obj, label in pairs(robLabels) do
        if getgenv().EliteConfig.RobberyESP and obj and obj.Parent then
            local pos, screen = _CAMERA:WorldToViewportPoint(obj.Position)
            if screen then
                local dist = (_LP.Character.HumanoidRootPart.Position - obj.Position).Magnitude
                if dist < 2000 then
                    -- เช็คสถานะ EH: ปกติถ้าปล้นไม่ได้ EH จะมี Cooldown Part หรือความโปร่งใสเปลี่ยน
                    local canRob = true
                    if obj:FindFirstChild("Cooldown") or obj.Transparency > 0.5 then canRob = false end
                    
                    label.Position = Vector2.new(pos.X, pos.Y)
                    label.Text = "[" .. obj.Name .. "]\n" .. (canRob and "READY" or "COOLDOWN") .. "\n[" .. math.floor(dist) .. "m]"
                    label.Color = canRob and Color3.new(0,1,0) or Color3.new(1,0,0)
                    label.Visible = true
                else label.Visible = false end
            else label.Visible = false end
        else label.Visible = false end
    end
end)

-- Scan Robbery Points Every 10s
spawn(function() while wait(10) do pcall(ScanRobbery) end end)

-- // Vehicle Boost //
_RUN.Heartbeat:Connect(function()
    if getgenv().EliteConfig.VehicleTune then
        local char = _LP.Character
        if char and char:FindFirstChild("Humanoid") and char.Humanoid.SeatPart then
            local seat = char.Humanoid.SeatPart
            if seat:IsA("VehicleSeat") and seat.Throttle == 1 then
                seat.Parent:TranslateBy(seat.CFrame.LookVector * (getgenv().EliteConfig.SpeedMult / 10))
            end
        end
    end
end)

-- // Criminal ESP System //
local function CreateESP(p)
    local box = Drawing.new("Square"); box.Thickness = 1; box.Visible = false
    local txt = Drawing.new("Text"); txt.Size = 16; txt.Outline = true; txt.Center = true; txt.Visible = false
    txt.Color = Color3.new(1, 1, 1)

    _RUN.RenderStepped:Connect(function()
        if getgenv().EliteConfig.ESPEnabled and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
            local dist = (_LP.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
            if dist <= getgenv().EliteConfig.MaxDist then
                local pos, screen = _CAMERA:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
                if screen then
                    local color = IsShootable(p) and Color3.new(1,0,0) or Color3.new(0,1,1)
                    box.Size = Vector2.new(50, 50); box.Position = Vector2.new(pos.X - 25, pos.Y - 25); box.Color = color; box.Visible = true
                    txt.Position = Vector2.new(pos.X, pos.Y - 50); txt.Text = (IsShootable(p) and "[TARGET]\n" or "") .. p.DisplayName .. "\n[" .. math.floor(dist) .. "m]"; txt.Visible = true
                else box.Visible = false; txt.Visible = false end
            else box.Visible = false; txt.Visible = false end
        else box.Visible = false; txt.Visible = false end
    end)
end
for _, p in pairs(game.Players:GetPlayers()) do if p ~= _LP then CreateESP(p) end end
game.Players.PlayerAdded:Connect(function(p) if p ~= _LP then CreateESP(p) end end)
