--[[ 
    BUKWAVE x GROK VOID v2.0 KAVO UI - Stable for Arceus X
    Aimbot + Silent + Gun + Heal + ESP สีทีม + Floating Button
--]]

local Kavo = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Kavo:CreateLib("BUKWAVE x GROK VOID v2.0", "DarkTheme")

-- Config
local Config = {
    AutoShoot = false, ShootFOV = 130,
    SilentBullet = false, SilentPart = "Head", SilentFOV = 130, HitChance = 90,
    NoRecoil = true, NoSpread = true, InfAmmo = true, FastReload = true, RapidFire = true, FireMultiplier = 2.5,
    EnableESP = true, MaxDist = 1500,
    CarTuning = false, CarSpeed = 500, CarTurbo = false, CarGod = true,
    AutoHeal = false, HealThreshold = 19, HealPos = Vector3.new(310.5, 12.0, 60.5)
}

-- Services & Functions (เหมือนเดิม)
local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local UIS = game:GetService("UserInputService")
local RS = game:GetService("RunService")
local WS = workspace

local function IsAlive(plr) local c = plr.Character return c and c:FindFirstChild("Humanoid") and c.Humanoid.Health > 0 end

local function GetClosest(fov, teamCheck, partName)
    local t, md = nil, fov
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LP and IsAlive(p) and (not teamCheck or p.Team ~= LP.Team) then
            local part = p.Character and p.Character:FindFirstChild(partName or "Head")
            if part then
                local pos, on = Camera:WorldToViewportPoint(part.Position)
                if on then
                    local mousePos = UIS:GetMouseLocation() or Vector2.new(0,0)
                    local d = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                    if d < md then md, t = d, p end
                end
            end
        end
    end
    return t
end

-- ESP (สีทีม + เมตร)
local ESPs = {}
local function AddESP(p)
    if p == LP then return end
    local d = {}
    d.Box = Drawing.new("Square"); d.Box.Thickness = 1; d.Box.Filled = false
    d.Name = Drawing.new("Text"); d.Name.Size = 14; d.Name.Center = true; d.Name.Outline = true
    d.Dist = Drawing.new("Text"); d.Dist.Size = 12; d.Dist.Outline = true
    ESPs[p] = d
end

local function RemESP(p)
    if ESPs[p] then for _,o in pairs(ESPs[p]) do o:Remove() end; ESPs[p] = nil end
end

local function UpESP()
    for p, d in pairs(ESPs) do
        if not IsAlive(p) or not Config.EnableESP then for _,o in pairs(d) do o.Visible = false end continue end
        local char = p.Character
        if not char then continue end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root or not root.Position then continue end
        
        local dist = (LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") and (LP.Character.HumanoidRootPart.Position - root.Position).Magnitude) or 9999
        if dist > Config.MaxDist then for _,o in pairs(d) do o.Visible = false end continue end
        
        local pos, v = Camera:WorldToViewportPoint(root.Position)
        if not v then for _,o in pairs(d) do o.Visible = false end continue end
        
        local h = math.clamp(500 / pos.Z, 20, 180)
        local w = h * 0.6
        
        local teamColor = Color3.new(1,1,1)
        if p.Team then
            local tn = p.Team.Name:lower()
            if tn:find("police") or tn:find("cop") then teamColor = Color3.fromRGB(0,100,255) end
            if tn:find("robber") or tn:find("criminal") then teamColor = Color3.fromRGB(255,0,0) end
        end
        
        if Config.ESP_Box then d.Box.Size = Vector2.new(w,h); d.Box.Position = Vector2.new(pos.X-w/2,pos.Y-h/2); d.Box.Color = teamColor; d.Box.Visible = true end
        if Config.ESP_NameTeam then d.Name.Text = p.Name..(p.Team and " ["..p.Team.Name.."]" or ""); d.Name.Position = Vector2.new(pos.X,pos.Y-h/2-20); d.Name.Color = teamColor; d.Name.Visible = true end
        if Config.ESP_Dist then d.Dist.Text = math.floor(dist).."m"; d.Dist.Position = Vector2.new(pos.X,pos.Y+h/2+10); d.Dist.Color = teamColor; d.Dist.Visible = true end
    end
end

for _,v in ipairs(Players:GetPlayers()) do AddESP(v) end
Players.PlayerAdded:Connect(AddESP)
Players.PlayerRemoving:Connect(RemESP)
RS.Heartbeat:Connect(UpESP)

-- Hooks (Gun + Silent)
local mt = getrawmetatable(game)
setreadonly(mt, false)
local oldNamecall = mt.__namecall
mt.__namecall = newcclosure(function(self, ...)
    local args = {...}
    local method = getnamecallmethod()
    if Config.SilentBullet and self == WS and method == "Raycast" then
        local dir = args[2]
        if dir.Magnitude < 50 then return oldNamecall(self, ...) end
        local target = GetClosest(Config.SilentFOV, Config.SilentTeamCheck, Config.SilentPart)
        if target and math.random(1,100) <= Config.HitChance then
            local tp = target.Character[Config.SilentPart].Position + (target.Character.HumanoidRootPart.Velocity * 0.135)
            args[2] = (tp - args[1]).Unit * dir.Magnitude
        end
    end
    return oldNamecall(self, unpack(args))
end)

local oldIndex = mt.__index
mt.__index = newcclosure(function(self, key)
    if not checkcaller() then
        if self:IsA("Tool") then
            if key == "Recoil" and Config.NoRecoil then return 0 end
            if key == "Spread" and Config.NoSpread then return 0 end
            if key == "Ammo" and Config.InfAmmo then return math.huge end
            if key == "ReloadTime" and Config.FastReload then return 0 end
            if key == "FireRate" and Config.RapidFire then return 0.15 / Config.FireMultiplier end
        end
    end
    return oldIndex(self, key)
end)
setreadonly(mt, true)

-- Loops
RS.Heartbeat:Connect(function()
    if Config.AutoHeal and IsAlive(LP) and LP.Character.Humanoid.Health < Config.HealThreshold then
        LP.Character.HumanoidRootPart.CFrame = CFrame.new(Config.HealPos)
    end
end)

-- Kavo Tabs
local Tab1 = Window:NewTab("SHOOT")
local Tab1Section = Tab1:NewSection("Aimbot & Silent")
Tab1Section:NewToggle("Auto Shoot (Aimbot)", "Auto aim + shoot", function(v) Config.AutoShoot = v end)
Tab1Section:NewToggle("Silent Bullet", "Silent Aim", function(v) Config.SilentBullet = v end)
Tab1Section:NewSlider("Shoot FOV", "FOV for Auto Shoot", 300, 50, function(v) Config.ShootFOV = v end)

local Tab2 = Window:NewTab("GUN MODS")
local Tab2Section = Tab2:NewSection("Weapon Mods")
Tab2Section:NewToggle("Rapid Fire", "", function(v) Config.RapidFire = v end)
Tab2Section:NewSlider("Fire Multiplier", "1-5x", 5, 1, function(v) Config.FireMultiplier = v end)
Tab2Section:NewToggle("No Recoil", "", function(v) Config.NoRecoil = v end)
Tab2Section:NewToggle("No Spread", "", function(v) Config.NoSpread = v end)
Tab2Section:NewToggle("Inf Ammo", "", function(v) Config.InfAmmo = v end)
Tab2Section:NewToggle("Fast Reload", "", function(v) Config.FastReload = v end)

local Tab3 = Window:NewTab("ESP")
local Tab3Section = Tab3:NewSection("ESP Settings")
Tab3Section:NewToggle("Enable ESP", "", function(v) Config.EnableESP = v end)
Tab3Section:NewSlider("Max Distance", "meters", 3000, 500, function(v) Config.MaxDist = v end)

local Tab4 = Window:NewTab("AUTO HEAL")
local Tab4Section = Tab4:NewSection("Heal Settings")
Tab4Section:NewToggle("Auto Heal", "TP when HP low", function(v) Config.AutoHeal = v end)
Tab4Section:NewSlider("Heal Threshold", "HP < x", 50, 10, function(v) Config.HealThreshold = v end)

Kavo:ToggleUI()  -- เปิด UI ครั้งแรก

print("[DEBUG] Kavo UI loaded - Stable version!")
