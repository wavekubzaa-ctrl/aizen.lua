--[[ 
    PROJECT: ELITE HAMBURG (V57 - FORCE UI FIX)
    FIXES: UI Not Showing, Overlap Issues, Target Logic
    LOGIC: Simplified UI initialization for maximum compatibility
--]]

-- // Check and Clear existing UI //
local function ClearOldUI()
    for _, ui in pairs(game.CoreGui:GetChildren()) do
        if ui.Name == "Elite_V57" or ui.Name == "Elite_V56_Stable" or ui:FindFirstChild("MainButton") then
            ui:Destroy()
        end
    end
end
ClearOldUI()

local _LP = game.Players.LocalPlayer
local _CAMERA = workspace.CurrentCamera
local _RUN = game:GetService("RunService")
local _LIGHTING = game:GetService("Lighting")

getgenv().EliteConfig = {
    AimEnabled = false,
    AutoShoot = false,
    AimPart = "HumanoidRootPart", 
    FOV = 200,
    CurrentMode = "G36",
    BulletSpeed = 1100,
    Smoothness = 0.08,
    ArrestAssist = false,
    VehicleBoost = false,
    NoFog = false,
    SpeedValue = 300,
    AdminNotify = true,
    ThemeColor = Color3.fromRGB(255, 0, 100)
}

local WeaponSettings = {
    ["G36"] = {Speed = 1100, Color = Color3.fromRGB(0, 255, 200)},
    ["Sniper"] = {Speed = 3500, Color = Color3.fromRGB(255, 50, 50)},
    ["Glock 17"] = {Speed = 950, Color = Color3.fromRGB(255, 200, 0)}
}

-- // UI Base //
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Elite_V57"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

-- // Toggle Button //
local MainButton = Instance.new("TextButton")
MainButton.Name = "MainButton"
MainButton.Parent = ScreenGui
MainButton.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainButton.Position = UDim2.new(0, 20, 0.4, 0)
MainButton.Size = UDim2.new(0, 60, 0, 60)
MainButton.Font = Enum.Font.GothamBold
MainButton.Text = "ELITE\nV57"
MainButton.TextColor3 = getgenv().EliteConfig.ThemeColor
MainButton.TextSize = 14
MainButton.Draggable = true
Instance.new("UICorner", MainButton).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", MainButton).Color = getgenv().EliteConfig.ThemeColor

-- // Main Panel //
local Panel = Instance.new("Frame")
Panel.Parent = MainButton
Panel.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Panel.Position = UDim2.new(1, 15, 0, -150)
Panel.Size = UDim2.new(0, 210, 0, 400)
Panel.Visible = false
Instance.new("UICorner", Panel)

local Scroll = Instance.new("ScrollingFrame")
Scroll.Parent = Panel
Scroll.Size = UDim2.new(1, -10, 1, -20)
Scroll.Position = UDim2.new(0, 5, 0, 10)
Scroll.BackgroundTransparency = 1
Scroll.CanvasSize = UDim2.new(0, 0, 0, 500)
Scroll.ScrollBarThickness = 2

local UIList = Instance.new("UIListLayout")
UIList.Parent = Scroll
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 5)

-- // Button Builder //
local function CreateButton(text, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = Scroll
    btn.Size = UDim2.new(1, -10, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    btn.Font = Enum.Font.SourceSansBold
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.TextSize = 16
    Instance.new("UICorner", btn)
    btn.MouseButton1Click:Connect(function() callback(btn) end)
    return btn
end

-- // Add Features //
CreateButton("AIMLOCK: OFF", function(b)
    getgenv().EliteConfig.AimEnabled = not getgenv().EliteConfig.AimEnabled
    b.Text = getgenv().EliteConfig.AimEnabled and "AIMLOCK: ON" or "AIMLOCK: OFF"
end)

CreateButton("SMOOTH: 0.08 (FAST)", function(b)
    getgenv().EliteConfig.Smoothness = (getgenv().EliteConfig.Smoothness == 0.08) and 0.15 or 0.08
    b.Text = "SMOOTH: " .. getgenv().EliteConfig.Smoothness .. (getgenv().EliteConfig.Smoothness == 0.08 and " (FAST)" or " (LEGIT)")
end)

local Modes = {"G36", "Sniper", "Glock 17"}
local mIdx = 1
CreateButton("MODE: G36", function(b)
    mIdx = (mIdx % #Modes) + 1
    local m = Modes[mIdx]
    getgenv().EliteConfig.CurrentMode = m
    getgenv().EliteConfig.BulletSpeed = WeaponSettings[m].Speed
    b.Text = "MODE: " .. m
    MainButton.TextColor3 = WeaponSettings[m].Color
end)

CreateButton("VEHICLE BOOST: OFF", function(b)
    getgenv().EliteConfig.VehicleBoost = not getgenv().EliteConfig.VehicleBoost
    b.Text = "BOOST: " .. (getgenv().EliteConfig.VehicleBoost and "ON" or "OFF")
end)

CreateButton("ARREST ASSIST: OFF", function(b)
    getgenv().EliteConfig.ArrestAssist = not getgenv().EliteConfig.ArrestAssist
    b.Text = "ARREST: " .. (getgenv().EliteConfig.ArrestAssist and "ON" or "OFF")
end)

CreateButton("NO FOG: OFF", function(b)
    getgenv().EliteConfig.NoFog = not getgenv().EliteConfig.NoFog
    b.Text = "NO FOG: " .. (getgenv().EliteConfig.NoFog and "ON" or "OFF")
end)

CreateButton("SAFE EXIT", function() _LP:Kick("Safe Exit Triggered") end)

MainButton.MouseButton1Click:Connect(function() Panel.Visible = not Panel.Visible end)

-- // Targeting & Logic //
local function GetTargetable(p)
    if not p.Character or not p.Character:FindFirstChild("Humanoid") or p.Character.Humanoid.Health <= 0 then return false end
    if p.Team == _LP.Team then return false end
    return true -- Simplified for stability
end

local ESP_Table = {}
local function AddESP(p)
    local t = Drawing.new("Text")
    t.Visible = false; t.Center = true; t.Outline = true; t.Font = 2; t.Size = 13
    ESP_Table[p] = t
end

for _, p in pairs(game.Players:GetPlayers()) do if p ~= _LP then AddESP(p) end end
game.Players.PlayerAdded:Connect(function(p) if p ~= _LP then AddESP(p) end end)

-- // Main Update Loop //
_RUN.RenderStepped:Connect(function()
    if getgenv().EliteConfig.NoFog then
        _LIGHTING.FogEnd = 999999
        if _LIGHTING:FindFirstChild("Atmosphere") then _LIGHTING.Atmosphere.Density = 0 end
    end

    for p, t in pairs(ESP_Table) do
        if p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
            local pos, screen = _CAMERA:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
            if screen then
                t.Position = Vector2.new(pos.X, pos.Y - 40)
                t.Text = p.DisplayName .. " [" .. math.floor((_LP.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude) .. "m]"
                t.Visible = true; t.Color = Color3.new(1, 1, 1)
            else t.Visible = false end
        else t.Visible = false end
    end

    if getgenv().EliteConfig.AimEnabled then
        local tool = _LP.Character and _LP.Character:FindFirstChildWhichIsA("Tool")
        if tool and (tool.Name:lower():find("gun") or tool.Name:lower():find("glock") or tool.Name:lower():find("rifle")) then
            local target, shortestMag = nil, getgenv().EliteConfig.FOV
            for _, p in pairs(game.Players:GetPlayers()) do
                if p ~= _LP and GetTargetable(p) then
                    local tPart = p.Character:FindFirstChild(getgenv().EliteConfig.AimPart)
                    if tPart then
                        local pos, screen = _CAMERA:WorldToViewportPoint(tPart.Position)
                        if screen then
                            local mag = (Vector2.new(pos.X, pos.Y) - Vector2.new(_CAMERA.ViewportSize.X/2, _CAMERA.ViewportSize.Y/2)).Magnitude
                            if mag < shortestMag then shortestMag = mag; target = p end
                        end
                    end
                end
            end
            if target then
                local tPart = target.Character[getgenv().EliteConfig.AimPart]
                local dist = (_LP.Character.HumanoidRootPart.Position - tPart.Position).Magnitude
                local predictPos = tPart.Position + (tPart.AssemblyLinearVelocity * (dist / getgenv().EliteConfig.BulletSpeed))
                _CAMERA.CFrame = _CAMERA.CFrame:Lerp(CFrame.new(_CAMERA.CFrame.Position, predictPos + Vector3.new(0, dist/750, 0)), getgenv().EliteConfig.Smoothness)
            end
        end
    end
end)

_RUN.Heartbeat:Connect(function()
    if getgenv().EliteConfig.VehicleBoost then
        local seat = _LP.Character and _LP.Character:FindFirstChild("Humanoid") and _LP.Character.Humanoid.SeatPart
        if seat and seat:IsA("VehicleSeat") and seat.Throttle ~= 0 then
            seat.AssemblyLinearVelocity = seat.CFrame.LookVector * (getgenv().EliteConfig.SpeedValue * seat.Throttle) + Vector3.new(0, seat.AssemblyLinearVelocity.Y, 0)
        end
    end
end)
