--[[ 
    bukwaveHUB Mobile Lite PRO
    SUPPORT: Mobile & Tablet (Touch Optimized)
    FEATURES: Toggle Aimlock, Team ESP, Selectable AimPart
--]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- // Global Config //
getgenv().Config = {
    AimEnabled = false,
    AimPart = "HumanoidRootPart",
    AimSmoothness = 0.15, -- เพิ่มความหน่วงนิดหน่อยเพื่อให้ดูเนียนบนมือถือ
    EspEnabled = false,
    MainColor = Color3.fromRGB(200, 0, 0)
}

-- // UI Creation (Mobile Optimized) //
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "BWH_Mobile_Lite"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 160, 0, 200) -- ขนาดพอดีสำหรับนิ้ว
MainFrame.Position = UDim2.new(0.05, 0, 0.4, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Active = true
MainFrame.Draggable = true -- ลากได้อิสระ
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = getgenv().Config.MainColor
Stroke.Thickness = 2

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "BH MOBILE"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.BackgroundTransparency = 1

-- // ESP Logic (Mobile Friendly) //
local function CreateESP(player)
    local NameTag = Drawing.new("Text")
    NameTag.Visible = false
    NameTag.Color = Color3.new(1, 1, 1)
    NameTag.Size = 14
    NameTag.Center = true
    NameTag.Outline = true

    local Connection
    Connection = RunService.RenderStepped:Connect(function()
        if getgenv().Config.EspEnabled and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player ~= LocalPlayer then
            local Root = player.Character.HumanoidRootPart
            local Pos, OnScreen = Camera:WorldToViewportPoint(Root.Position)
            
            if OnScreen then
                local Dist = math.floor((Root.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude)
                local TeamName = player.Team and player.Team.Name or "No Team"
                NameTag.Text = string.format("%s\n[%s]\n%dm", player.Name, TeamName, Dist)
                NameTag.Position = Vector2.new(Pos.X, Pos.Y - 50)
                NameTag.Color = player.TeamColor.Color
                NameTag.Visible = true
            else
                NameTag.Visible = false
            end
        else
            NameTag.Visible = false
            if not player.Parent or not getgenv().Config then
                NameTag:Remove()
                Connection:Disconnect()
            end
        end
    end)
end

for _, p in pairs(Players:GetPlayers()) do CreateESP(p) end
Players.PlayerAdded:Connect(CreateESP)

-- // Aimlock Logic (Auto-Lock for Mobile) //
local function GetClosest()
    local target, dist = nil, 250 -- รัศมีการตรวจจับ
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild(getgenv().Config.AimPart) and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
            local Pos, OnScreen = Camera:WorldToViewportPoint(p.Character[getgenv().Config.AimPart].Position)
            if OnScreen then
                local Mag = (Vector2.new(Pos.X, Pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if Mag < dist then
                    dist = Mag
                    target = p
                end
            end
        end
    end
    return target
end

RunService.RenderStepped:Connect(function()
    if getgenv().Config.AimEnabled then
        local t = GetClosest()
        if t then
            local targetPos = t.Character[getgenv().Config.AimPart].Position
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, targetPos), getgenv().Config.AimSmoothness)
        end
    end
end)

-- // Button Builder //
local function AddButton(text, yPos, callback)
    local b = Instance.new("TextButton", MainFrame)
    b.Size = UDim2.new(0.85, 0, 0, 35)
    b.Position = UDim2.new(0.075, 0, 0, yPos)
    b.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    b.Text = text
    b.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    b.Font = Enum.Font.GothamMedium
    b.TextSize = 12
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
    
    b.MouseButton1Click:Connect(function()
        callback(b)
    end)
end

-- // Controls //
AddButton("AIMLOCK: OFF", 45, function(btn)
    getgenv().Config.AimEnabled = not getgenv().Config.AimEnabled
    btn.Text = getgenv().Config.AimEnabled and "AIMLOCK: ON" or "AIMLOCK: OFF"
    btn.BackgroundColor3 = getgenv().Config.AimEnabled and getgenv().Config.MainColor or Color3.fromRGB(30, 30, 30)
end)

AddButton("ESP: OFF", 85, function(btn)
    getgenv().Config.EspEnabled = not getgenv().Config.EspEnabled
    btn.Text = getgenv().Config.EspEnabled and "ESP: ON" or "ESP: OFF"
    btn.BackgroundColor3 = getgenv().Config.EspEnabled and getgenv().Config.MainColor or Color3.fromRGB(30, 30, 30)
end)

AddButton("TARGET: BODY", 125, function(btn)
    if getgenv().Config.AimPart == "HumanoidRootPart" then
        getgenv().Config.AimPart = "Head"
        btn.Text = "TARGET: HEAD"
    else
        getgenv().Config.AimPart = "HumanoidRootPart"
        btn.Text = "TARGET: BODY"
    end
end)

-- // Minimize Button //
local MinBtn = Instance.new("TextButton", MainFrame)
MinBtn.Size = UDim2.new(0, 25, 0, 25)
MinBtn.Position = UDim2.new(1, -30, 0, 7)
MinBtn.Text = "-"
MinBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MinBtn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", MinBtn)

local isMin = false
MinBtn.MouseButton1Click:Connect(function()
    isMin = not isMin
    for _, v in pairs(MainFrame:GetChildren()) do
        if v:IsA("TextButton") and v ~= MinBtn then v.Visible = not isMin end
    end
    MainFrame:TweenSize(isMin and UDim2.new(0, 160, 0, 40) or UDim2.new(0, 160, 0, 200), "Out", "Quart", 0.2, true)
end)

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "BH Mobile Lite",
    Text = "Touch buttons to toggle features!",
    Duration = 5
})
